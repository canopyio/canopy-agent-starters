{
  "name": "canopy-research-agent",
  "private": true,
  "version": "0.1.0",
  "description": "Canopy starter: multi-source research agent that pays for gated data APIs and premium sources via policy-gated USDC payments.",
  "type": "module",
  "scripts": {
    "start": "tsx src/main.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
